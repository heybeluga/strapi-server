'use strict';

/**
 * ecosystem-activity router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ecosystem-activity.ecosystem-activity');
