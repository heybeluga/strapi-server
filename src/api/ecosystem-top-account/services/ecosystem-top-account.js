'use strict';

/**
 * ecosystem-top-account service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ecosystem-top-account.ecosystem-top-account');
